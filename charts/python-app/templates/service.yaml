apiVersion: v1
kind: Service
metadata:
  name: {{ include "py6app.fullname" . }}
  labels:
    {{- include "py6app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "py6app.selectorLabels" . | nindent 4 }}
